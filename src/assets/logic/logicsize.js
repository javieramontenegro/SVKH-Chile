/* export const biggerSize = () => {
  let normalSize = document.getElementById("body");
  if(normalSize.getAttribute("class") === "normal-size"){
    normalSize.classList.remove("normal-size");
    normalSize.classList.add("bigger-size");
  }
  else {
    normalSize.classList.remove("bigger-size");
    normalSize.classList.add("normal-size");
  }
} */